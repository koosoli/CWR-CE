# Trident environment — loaded automatically by `trident` CLI.
# Copy to .trident.env and adjust paths for your setup.
# CLI flags and explicit env vars override these values.

OFPR_GAME_DIR=dist/x64-win-rwdi
OFPR_DATA_DIR=packages/Demo
