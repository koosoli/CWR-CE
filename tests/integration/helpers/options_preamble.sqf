// Navigate from the main menu to the Options shell (IDD 9099).
// Language is English by default (the runner injects --lang English),
// so no triSetLanguage call is needed.
triClickText "OPTIONS"
triAssertDisplay 9099
